# Урок 10 - Закрепление ООП

## 📚 Описание

Этот урок помогает закрепить базовые концепции Python: функции, циклы, условия и простое ООП (классы, наследование) на примере магазина электронной техники.

## 🎯 Что изучаем

### 1. **Классы и объекты** 📦
- Создание классов с помощью `class`
- Конструктор `__init__` для инициализации объектов
- Приватные поля с подчеркиванием (`_name`, `_price`)
- Создание объектов: `product = Product(...)`

### 2. **Наследование** 🌳
- Базовый класс `Product`
- Конкретный класс: `Smartphone` (пример наследования)
- Использование `super()` для вызова методов родителя
- Переопределение методов в подклассах

### 3. **Функции** 🔧
- Создание функций с помощью `def`
- Параметры функций
- Возврат значений через `return`
- Использование функций для организации кода

### 4. **Циклы** 🔄
- Цикл `for` для перебора списков
- Использование циклов для обработки данных
- Подсчет сумм и количеств

### 5. **Условия** ⚖️
- Условные операторы `if`, `elif`, `else`
- Проверка условий
- Валидация данных

## 📁 Структура проекта

```
lesson_10_OOP_advanced/
├── __init__.py           # Пакет Python
├── oop_10.py            # Основные классы с ООП
├── README.md            # Этот файл
└── homework/
    ├── homework_10.md   # Задания
    └── homework_10.py   # Решения (создай сам)
```

## 🚀 Запуск

### Демонстрация ООП

```bash
# Активируй виртуальное окружение
source .venv/bin/activate

# Запусти демонстрацию
python lesson_10_OOP_advanced/oop_10.py
```

### Домашнее задание

```bash
# Активируй виртуальное окружение
source .venv/bin/activate

# Запусти свой файл с решениями
python lesson_10_OOP_advanced/homework/homework_10.py
```

## 🔧 Основные классы

### Product (базовый)
Базовый класс для всех товаров:
- Поля: `id`, `name`, `price`, `stock`, `discount_percent`
- Методы: `get_final_price()`, `is_available()`, `add_stock()`, `reduce_stock()`
- Методы для переопределения: `get_category()`, `get_specifications()`, `get_warranty_period()`

### Smartphone
Конкретный класс товара (пример наследования):
- Наследует `Product`
- Добавляет специфичные поля (`brand`, `model`, `battery_mah` и др.)
- Переопределяет методы родителя
- Имеет уникальные методы (например, `get_battery_life_estimate()`)

### Inventory
Управление инвентарем:
- Хранит все товары
- Методы добавления, получения, удаления товаров
- Фильтрация по категориям
- Применение скидок к категориям
- Подсчет общей стоимости

## 💡 Примеры использования

### Создание товара

```python
from lesson_10_OOP_advanced.oop_10 import Smartphone

# Создаем смартфон
phone = Smartphone(
    name="iPhone 15 Pro",
    price=99990,
    brand="Apple",
    model="15 Pro",
    screen_size=6.1,
    ram_gb=8,
    storage_gb=256,
    battery_mah=3274,
    stock=15
)

# Используем свойства (геттеры)
print(phone.name)  # iPhone 15 Pro
print(phone.price)  # 99990
print(phone.stock)  # 15

# Используем методы
print(phone.get_category())  # ProductCategory.SMARTPHONE
print(phone.get_specifications())  # {'brand': 'Apple', 'model': '15 Pro', ...}
print(phone.get_battery_life_estimate())  # Среднее (до 1 дня)

# Применяем скидку
phone.discount_percent = 10
print(phone.get_final_price())  # 89991.0 (цена со скидкой)
```

### Работа с инвентарем

```python
from lesson_10_OOP_advanced.oop_10 import Inventory, ProductCategory

inventory = Inventory()
inventory.add_product(phone)

# Получить все товары
all_products = inventory.get_all_products()

# Получить товары по категории
smartphones = inventory.get_products_by_category(ProductCategory.SMARTPHONE)

# Применить скидку к категории
inventory.apply_discount_to_category(ProductCategory.SMARTPHONE, 15)
```

## 🎓 Что изучить

1. **Классы и объекты** - как создавать и использовать
2. **Наследование** - создание иерархии классов
3. **Функции** - создание и использование функций
4. **Циклы** - цикл `for` для перебора данных
5. **Условия** - операторы `if`, `elif`, `else`
6. **Инкапсуляция** - приватные поля
7. **Enum** - перечисления для категорий

## 🏆 Задачи для практики

Смотри файл `homework/homework_10.md` для заданий!

## 📝 Заметки

- Код простой и понятный для начинающих
- Можно легко расширить новыми типами товаров
- Легко добавить новые методы и функциональность
- Хорошая структура для изучения ООП
