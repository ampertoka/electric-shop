# Урок 10 - Закрепление ООП с FastAPI

## 📚 Описание

Этот урок демонстрирует все принципы ООП на примере магазина электронной техники с полной интеграцией FastAPI.

## 🎯 Принципы ООП в проекте

### 1. **Инкапсуляция** 🔒
- Приватные поля с подчеркиванием (`_name`, `_price`)
- Геттеры и сеттеры через `@property`
- Валидация данных при установке значений
- Пример: `product.price = 1000` → автоматическая валидация

### 2. **Наследование** 🌳
- Базовый абстрактный класс `Product`
- Конкретные классы: `Smartphone`, `Laptop`, `Tablet`, `Cable`, `Accessory`
- Использование `super()` для вызова методов родителя
- Расширение функциональности в подклассах

### 3. **Полиморфизм** 🎭
- Один интерфейс - разные реализации
- Методы `get_category()`, `get_specifications()`, `get_warranty_period()`
- Каждый класс реализует их по-своему
- Можно работать с разными товарами через единый интерфейс

### 4. **Абстракция** 🎨
- Абстрактный класс `Product` (ABC)
- Абстрактные методы с декоратором `@abstractmethod`
- Нельзя создать экземпляр абстрактного класса
- Подклассы обязаны реализовать абстрактные методы

## 📁 Структура проекта

```
lesson_10_OOP_advanced/
├── __init__.py           # Пакет Python
├── oop_10.py            # Основные классы с ООП
├── fastapi_app.py       # FastAPI приложение
├── README.md            # Этот файл
└── homework/
    ├── homework_10.md   # Задания
    └── homework_10.py   # Решения (создай сам)
```

## 🚀 Запуск

### 1. Демонстрация ООП (без FastAPI)

```bash
# Активируй виртуальное окружение
source .venv/bin/activate

# Запусти демонстрацию
python lesson_10_OOP_advanced/oop_10.py
```

### 2. FastAPI приложение

```bash
# Активируй виртуальное окружение
source .venv/bin/activate

# Запусти сервер
uvicorn lesson_10_OOP_advanced.fastapi_app:app --reload

# Открой в браузере:
# http://localhost:8000/docs - Swagger UI
# http://localhost:8000/redoc - ReDoc
```

## 🔧 Основные классы

### Product (абстрактный)
Базовый класс для всех товаров:
- Поля: `id`, `name`, `price`, `stock`, `discount_percent`
- Методы: `get_final_price()`, `is_available()`, `add_stock()`, `reduce_stock()`
- Абстрактные методы: `get_category()`, `get_specifications()`, `get_warranty_period()`

### Smartphone, Laptop, Tablet, Cable, Accessory
Конкретные классы товаров:
- Наследуют `Product`
- Добавляют специфичные поля (например, `battery_mah` для смартфона)
- Реализуют абстрактные методы
- Могут иметь свои уникальные методы

### Inventory
Управление инвентарем:
- Хранит все товары
- Методы поиска, фильтрации
- Применение скидок к категориям
- Подсчет общей стоимости

### Order и OrderItem
Система заказов:
- Композиция: Order содержит OrderItem
- OrderItem связывает товар и количество
- Автоматическое резервирование товара
- Управление статусами заказа

## 📡 API Endpoints

### Товары
- `POST /products` - Создать товар
- `GET /products` - Получить все товары (с фильтрами)
- `GET /products/{id}` - Получить товар по ID
- `PATCH /products/{id}` - Обновить товар
- `DELETE /products/{id}` - Удалить товар
- `POST /products/{id}/stock/add` - Добавить на склад
- `POST /products/category/{category}/discount` - Скидка на категорию

### Заказы
- `POST /orders` - Создать заказ
- `GET /orders` - Получить все заказы
- `GET /orders/{id}` - Получить заказ по ID
- `PATCH /orders/{id}/status` - Изменить статус
- `DELETE /orders/{id}` - Отменить заказ

### Статистика
- `GET /inventory/stats` - Статистика инвентаря
- `GET /orders/stats` - Статистика заказов

## 💡 Примеры использования

### Создание товара через API

```bash
curl -X POST "http://localhost:8000/products" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "iPhone 16 Pro",
    "price": 119990,
    "category": "smartphone",
    "brand": "Apple",
    "model": "16 Pro",
    "screen_size": 6.3,
    "ram_gb": 8,
    "storage_gb": 512,
    "battery_mah": 3500,
    "stock": 10
  }'
```

### Создание заказа

```bash
curl -X POST "http://localhost:8000/orders" \
  -H "Content-Type: application/json" \
  -d '{
    "customer_name": "Иван Иванов",
    "customer_email": "ivan@example.com",
    "items": [
      {"product_id": "PROD_00001", "quantity": 1},
      {"product_id": "PROD_00005", "quantity": 2}
    ]
  }'
```

## 🎓 Что изучить

1. **Классы и объекты** - как создавать и использовать
2. **Инкапсуляция** - приватные поля, геттеры/сеттеры
3. **Наследование** - создание иерархии классов
4. **Полиморфизм** - один интерфейс, разные реализации
5. **Абстракция** - абстрактные классы и методы
6. **Композиция** - объекты содержат другие объекты
7. **Enum** - перечисления для категорий и статусов
8. **Pydantic** - валидация данных в FastAPI
9. **Type hints** - аннотации типов в Python

## 🏆 Задачи для практики

Смотри файл `homework/homework_10.md` для заданий!

## 📝 Заметки

- Все классы следуют принципам SOLID
- Код готов к использованию в реальном приложении
- Можно легко расширить новыми типами товаров
- Легко добавить новые методы и функциональность
- Хорошая структура для масштабирования
