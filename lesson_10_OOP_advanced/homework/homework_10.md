# 📚 Домашка Урок 10 - Закрепление ООП

**Дата:** 29.11.2024  
**Формат:** Решения писать в файле `lesson_10_OOP_advanced/homework/homework_10.py`

---

## 🎯 Цель

Закрепить базовые концепции Python: функции, циклы, условия и простое ООП (классы, наследование).

---

## 📝 Задания

### 1️⃣ Создай новый тип товара - Monitor (Монитор)

**Задача:**
- Создай класс `Monitor`, который наследует `Product`
- Добавь специфичные поля:
  - `brand: str` - бренд
  - `screen_size: float` - диагональ в дюймах
  - `resolution: str` - разрешение (например, "1920x1080", "2560x1440")
  - `refresh_rate: int` - частота обновления в Гц
  - `panel_type: str` - тип матрицы (IPS, VA, TN)
- Переопредели методы родительского класса:
  - `get_category()` → верни `ProductCategory.MONITOR`
  - `get_specifications()` → верни словарь с характеристиками
  - `get_warranty_period()` → 24 месяца
- Добавь метод `is_gaming_monitor()` → возвращает `True`, если частота >= 144 Гц

**Проверка:**
```python
monitor = Monitor(
    name="LG UltraGear 27GL850",
    price=35990,
    brand="LG",
    screen_size=27,
    resolution="2560x1440",
    refresh_rate=144,
    panel_type="IPS",
    stock=8
)
print(monitor)
print(monitor.get_specifications())
print(f"Игровой: {monitor.is_gaming_monitor()}")
```

---

### 2️⃣ Создай класс SmartWatch (Умные часы)

**Задача:**
- Создай класс `SmartWatch`, который наследует `Product`
- В конструкторе `__init__` добавь параметры:
  - `brand: str` - бренд
  - `model: str` - модель
  - `display_size: float` - размер дисплея в дюймах
  - `battery_days: int` - время работы батареи в днях
  - `has_gps: bool` - наличие GPS (True/False)
  - `waterproof_rating: str` - защита от воды (например, "IP68", "5ATM")
- Сохрани эти значения в приватные поля (например, `self._brand`, `self._model`)
- Переопредели методы:
  - `get_category()` → верни `ProductCategory.ACCESSORY`
  - `get_specifications()` → верни словарь со всеми характеристиками
  - `get_warranty_period()` → 12 месяцев
- Добавь метод `is_suitable_for_sports()` → используй условие `if`: верни `True`, если `has_gps == True` и `waterproof_rating` не пустой

**Проверка:**
```python
watch = SmartWatch(
    name="Apple Watch Series 9",
    price=39990,
    brand="Apple",
    model="Series 9",
    display_size=1.9,
    battery_days=2,
    has_gps=True,
    waterproof_rating="5ATM",
    stock=12
)
print(watch.is_suitable_for_sports())  # Должно вывести True
```

---

### 3️⃣ Расширь класс Smartphone - добавь камеру

**Задача:**
- Создай класс `SmartphoneWithCamera`, который наследует `Smartphone`
- В конструкторе `__init__` добавь параметры:
  - `main_camera_mp: int` - мегапиксели основной камеры
  - `front_camera_mp: int` - мегапиксели фронтальной камеры
  - `has_optical_zoom: bool` - наличие оптического зума (True/False)
- Сохрани эти значения в приватные поля
- Переопредели метод `get_specifications()`:
  - Вызови `super().get_specifications()` для получения базовых характеристик смартфона
  - Добавь в словарь информацию о камерах (main_camera, front_camera, optical_zoom)
  - Верни обновленный словарь
- Добавь метод `get_camera_rating()` → используй условия `if-elif-else`:
  - "Отличная" если `main_camera_mp >= 48` и `has_optical_zoom == True`
  - "Хорошая" если `main_camera_mp >= 48`
  - "Средняя" в остальных случаях

---

### 4️⃣ Создай систему скидок - класс DiscountManager

**Задача:**
- Создай класс `DiscountManager` для управления скидками
- Методы:
  - `apply_seasonal_discount(inventory: Inventory, discount: float)` - применяет скидку ко всем товарам
  - `apply_clearance_discount(inventory: Inventory, min_stock: int, discount: float)` - применяет скидку к товарам с остатком меньше `min_stock`
  - `apply_premium_discount(inventory: Inventory, min_price: float, discount: float)` - скидка на дорогие товары (цена >= min_price)
  - `reset_all_discounts(inventory: Inventory)` - сбрасывает все скидки (устанавливает в 0)

**Проверка:**
```python
discount_mgr = DiscountManager()
discount_mgr.apply_clearance_discount(inventory, min_stock=10, discount=20)
# Все товары с остатком < 10 получат скидку 20%
```

---

### 5️⃣ Создай класс Customer (Покупатель)

**Задача:**
- Создай класс `Customer` с конструктором `__init__`, который принимает:
  - `name: str` - имя покупателя
  - `email: str` - email
  - `phone: str` - телефон
- В конструкторе создай приватные поля:
  - `self._id` - уникальный ID (можно использовать счетчик как в Product)
  - `self._name`, `self._email`, `self._phone` - сохрани переданные значения
  - `self._orders` - пустой список `[]` для хранения заказов
  - `self._loyalty_points` - начать с 0
- Методы:
  - `add_order(order: Order)` - добавляет заказ в список `_orders` и начисляет баллы:
    - Используй цикл: 1 балл за каждые 100₽ от суммы заказа
    - Например, если заказ на 250₽, то начисли 2 балла (250 // 100 = 2)
  - `get_total_spent()` - используй цикл `for` по списку `_orders`, суммируй все `order.get_total()`
  - `get_orders_count()` - верни длину списка `_orders` (используй `len()`)
  - `is_vip()` - используй условие `if`: верни `True` если `get_total_spent() >= 100000` или `get_orders_count() >= 10`

**Проверка:**
```python
customer = Customer(name="Иван Иванов", email="ivan@mail.ru", phone="+79991234567")
customer.add_order(order1)
customer.add_order(order2)
print(f"Потрачено: {customer.get_total_spent()}₽")
print(f"Баллов: {customer._loyalty_points}")
print(f"VIP: {customer.is_vip()}")
```

---

### 6️⃣ Функция для отчета о товарах

**Задача:**
- Создай функцию `generate_product_report(products: List[Product]) -> str`
- Функция должна:
  - Принимать список товаров
  - Использовать цикл `for` для перебора всех товаров
  - Для каждого товара получить: название (`product.name`), категорию (`product.get_category().value`), цену (`product.price`), гарантию (`product.get_warranty_period()`)
  - Использовать цикл для подсчета общей стоимости (суммируй `product.price` для каждого товара)
  - Использовать `len(products)` для подсчета количества товаров
  - Возвращать отформатированную строку с отчетом

**Пример вывода:**
```
=== ОТЧЕТ ПО ТОВАРАМ ===
1. iPhone 15 Pro [smartphone] - 99990₽ (гарантия: 12 мес.)
2. MacBook Pro 14 [laptop] - 189990₽ (гарантия: 24 мес.)
3. USB-C кабель [cable] - 1990₽ (гарантия: 6 мес.)
------------------------
Всего товаров: 3
Общая стоимость: 291970₽
```

**Подсказка:** Используй цикл `for i, product in enumerate(products, 1):` для нумерации товаров

---

### 7️⃣ Создай класс Review (Отзыв)

**Задача:**
- Создай класс `Review` с конструктором `__init__`, который принимает:
  - `product_id: str` - ID товара
  - `customer_name: str` - имя покупателя
  - `rating: int` - оценка от 1 до 5
  - `comment: str` - текст отзыва
- В конструкторе:
  - Проверь условием `if`: если `rating < 1` или `rating > 5`, выброси ошибку `ValueError("Рейтинг должен быть от 1 до 5")`
  - Проверь условием `if`: если `comment` пустой (используй `if not comment:`), выброси ошибку `ValueError("Комментарий не может быть пустым")`
  - Сохрани значения в приватные поля: `self._id` (можно использовать счетчик), `self._product_id`, `self._customer_name`, `self._rating`, `self._comment`, `self._created_at = datetime.now()`
- Методы:
  - `is_positive()` - используй условие `if`: верни `True` если `self._rating >= 4`
  - `to_dict()` - верни словарь со всеми полями отзыва

---

### 8️⃣ Создай классы для способов оплаты

**Задача:**
- Создай 3 простых класса для способов оплаты:
  - `CreditCardPayment` - оплата картой
  - `CashPayment` - оплата наличными
  - `CryptocurrencyPayment` - оплата криптовалютой
- Каждый класс должен иметь:
  - Конструктор `__init__` с нужными параметрами (например, для карты - `card_number: str`)
  - Метод `process_payment(amount: float)` - выводит сообщение о том, что оплата прошла (используй `print()`)
  - Метод `get_payment_type() -> str` - возвращает название способа оплаты
- Используй цикл `for` для демонстрации работы всех способов оплаты

**Проверка:**
```python
payments = [
    CreditCardPayment(card_number="1234-5678-9012-3456"),
    CashPayment(),
    CryptocurrencyPayment(wallet_address="0x123...")
]

for payment in payments:
    print(f"Способ оплаты: {payment.get_payment_type()}")
    payment.process_payment(1000)
```

---

### 9️⃣ Добавь промокоды

**Задача:**
- Создай класс `PromoCode` с конструктором `__init__`, который принимает:
  - `code: str` - код промокода
  - `discount_percent: float` - процент скидки (от 0 до 100)
  - `min_order_amount: float` - минимальная сумма заказа
  - `is_active: bool` - активен ли промокод (True/False)
- Сохрани все значения в приватные поля
- Добавь в класс `Order`:
  - В конструкторе добавь поле `self._applied_promo = None`
  - Метод `apply_promo_code(promo: PromoCode) -> bool`:
    - Используй условия `if`: проверь, что `promo.is_active == True`
    - Используй условие `if`: проверь, что `self.get_total() >= promo._min_order_amount`
    - Если оба условия выполнены - сохрани промокод в `self._applied_promo` и верни `True`
    - Иначе верни `False`
  - Измени метод `get_total()`:
    - Вычисли сумму заказа как обычно
    - Используй условие `if`: если `self._applied_promo` не `None`, примени скидку
    - Верни итоговую сумму

---

### 🔟 Добавь уведомления для заказов

**Задача:**
- Создай 3 простых класса для уведомлений:
  - `EmailNotification` - отправка email
  - `SMSNotification` - отправка SMS
  - `PushNotification` - push-уведомление
- Каждый класс должен иметь:
  - Метод `send_notification(message: str, recipient: str)` - выводит сообщение о том, что уведомление отправлено (используй `print()`)
- Добавь в класс `Order`:
  - В конструкторе добавь поле `self._notification_services = []` (пустой список)
  - Метод `add_notification_service(service)` - добавляет сервис в список (используй `append()`)
  - Измени метод `change_status()`:
    - Вызови оригинальный код изменения статуса
    - Используй цикл `for` по списку `_notification_services`
    - Для каждого сервиса вызови `send_notification()` с сообщением о смене статуса

**Пример:**
```python
order = Order("Иван", "ivan@mail.ru")
order.add_notification_service(EmailNotification())
order.add_notification_service(SMSNotification())

order.change_status(OrderStatus.SHIPPED)
# Отправит уведомления через email и SMS
```

---

---

## ✅ Критерии выполнения

- [ ] Все классы правильно используют наследование
- [ ] Используются функции, циклы и условия
- [ ] Используется инкапсуляция (приватные поля)
- [ ] Код работает без ошибок
- [ ] Код понятный и простой

---

## 💡 Подсказки

1. Импортируй классы из `oop_10.py`:
   ```python
   from lesson_10_OOP_advanced.oop_10 import Product, Inventory, Order, ProductCategory, Smartphone
   ```

2. Используй `super()` для вызова методов родительского класса

3. Не забывай про условия `if-elif-else` для проверок

4. Используй циклы `for` для перебора списков

5. Тестируй каждый класс отдельно перед интеграцией

---

## 🚀 Запуск

```bash
# Активируй виртуальное окружение
source .venv/bin/activate

# Запусти свой файл
python lesson_10_OOP_advanced/homework/homework_10.py
```

---

**Удачи! 🎉**
