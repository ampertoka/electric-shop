# 📚 Домашка Урок 10 - Закрепление ООП с FastAPI

**Дата:** 29.11.2024  
**Формат:** Решения писать в файле `lesson_10_OOP_advanced/homework/homework_10.py`

---

## 🎯 Цель

Закрепить все принципы ООП, создавая новые классы и расширяя существующую систему магазина электронной техники.

---

## 📝 Задания

### 1️⃣ Создай новый тип товара - Monitor (Монитор)

**Задача:**
- Создай класс `Monitor`, который наследует `Product`
- Добавь специфичные поля:
  - `brand: str` - бренд
  - `screen_size: float` - диагональ в дюймах
  - `resolution: str` - разрешение (например, "1920x1080", "2560x1440")
  - `refresh_rate: int` - частота обновления в Гц
  - `panel_type: str` - тип матрицы (IPS, VA, TN)
- Реализуй все абстрактные методы:
  - `get_category()` → верни `ProductCategory.ACCESSORY` (или создай новую категорию)
  - `get_specifications()` → верни словарь с характеристиками
  - `get_warranty_period()` → 24 месяца
- Добавь метод `is_gaming_monitor()` → возвращает `True`, если частота >= 144 Гц

**Проверка:**
```python
monitor = Monitor(
    name="LG UltraGear 27GL850",
    price=35990,
    brand="LG",
    screen_size=27,
    resolution="2560x1440",
    refresh_rate=144,
    panel_type="IPS",
    stock=8
)
print(monitor)
print(monitor.get_specifications())
print(f"Игровой: {monitor.is_gaming_monitor()}")
```

---

### 2️⃣ Создай класс SmartWatch (Умные часы)

**Задача:**
- Наследуй от `Product`
- Добавь поля:
  - `brand: str`
  - `model: str`
  - `display_size: float` - размер дисплея в дюймах
  - `battery_days: int` - время работы батареи в днях
  - `has_gps: bool` - наличие GPS
  - `waterproof_rating: str` - защита от воды (например, "IP68", "5ATM")
- Реализуй абстрактные методы
- Добавь метод `is_suitable_for_sports()` → `True`, если есть GPS и защита от воды

**Проверка:**
```python
watch = SmartWatch(
    name="Apple Watch Series 9",
    price=39990,
    brand="Apple",
    model="Series 9",
    display_size=1.9,
    battery_days=2,
    has_gps=True,
    waterproof_rating="5ATM",
    stock=12
)
print(watch.is_suitable_for_sports())
```

---

### 3️⃣ Расширь класс Smartphone - добавь камеру

**Задача:**
- Создай класс `SmartphoneWithCamera`, который наследует `Smartphone`
- Добавь новые поля:
  - `main_camera_mp: int` - мегапиксели основной камеры
  - `front_camera_mp: int` - мегапиксели фронтальной камеры
  - `has_optical_zoom: bool` - наличие оптического зума
- Переопредели метод `get_specifications()`:
  - Вызови `super().get_specifications()` для получения базовых характеристик
  - Добавь в словарь информацию о камерах
- Добавь метод `get_camera_rating()` → возвращает строку:
  - "Отличная" если основная камера >= 48 МП и есть оптический зум
  - "Хорошая" если >= 48 МП
  - "Средняя" в остальных случаях

---

### 4️⃣ Создай систему скидок - класс DiscountManager

**Задача:**
- Создай класс `DiscountManager` для управления скидками
- Методы:
  - `apply_seasonal_discount(inventory: Inventory, discount: float)` - применяет скидку ко всем товарам
  - `apply_clearance_discount(inventory: Inventory, min_stock: int, discount: float)` - применяет скидку к товарам с остатком меньше `min_stock`
  - `apply_premium_discount(inventory: Inventory, min_price: float, discount: float)` - скидка на дорогие товары (цена >= min_price)
  - `reset_all_discounts(inventory: Inventory)` - сбрасывает все скидки (устанавливает в 0)

**Проверка:**
```python
discount_mgr = DiscountManager()
discount_mgr.apply_clearance_discount(inventory, min_stock=10, discount=20)
# Все товары с остатком < 10 получат скидку 20%
```

---

### 5️⃣ Создай класс Customer (Покупатель)

**Задача:**
- Создай класс для покупателя с полями:
  - `_id: str` - уникальный ID (генерируется автоматически)
  - `_name: str`
  - `_email: str`
  - `_phone: str`
  - `_orders: List[Order]` - список заказов покупателя
  - `_loyalty_points: int` - бонусные баллы (по умолчанию 0)
- Используй инкапсуляцию (приватные поля + @property)
- Методы:
  - `add_order(order: Order)` - добавляет заказ и начисляет баллы (1 балл за каждые 100₽)
  - `get_total_spent()` - возвращает общую сумму всех заказов
  - `get_orders_count()` - количество заказов
  - `is_vip()` - VIP если потратил >= 100000₽ или >= 10 заказов
  - `to_dict()` - преобразует в словарь

**Проверка:**
```python
customer = Customer(name="Иван Иванов", email="ivan@mail.ru", phone="+79991234567")
customer.add_order(order1)
customer.add_order(order2)
print(f"Потрачено: {customer.get_total_spent()}₽")
print(f"Баллов: {customer.loyalty_points}")
print(f"VIP: {customer.is_vip()}")
```

---

### 6️⃣ Полиморфизм - функция для отчета

**Задача:**
- Создай функцию `generate_product_report(products: List[Product]) -> str`
- Функция должна:
  - Принимать список любых товаров (полиморфизм!)
  - Для каждого товара выводить: название, категорию, цену, гарантию
  - Подсчитывать общее количество товаров и их стоимость
  - Возвращать отформатированный отчет в виде строки

**Пример вывода:**
```
=== ОТЧЕТ ПО ТОВАРАМ ===
1. iPhone 15 Pro [smartphone] - 99990₽ (гарантия: 12 мес.)
2. MacBook Pro 14 [laptop] - 189990₽ (гарантия: 24 мес.)
3. USB-C кабель [cable] - 1990₽ (гарантия: 6 мес.)
------------------------
Всего товаров: 3
Общая стоимость: 291970₽
```

---

### 7️⃣ Создай класс Review (Отзыв)

**Задача:**
- Создай класс для отзывов на товары:
  - `_id: str` - ID отзыва
  - `_product_id: str` - ID товара
  - `_customer_name: str` - имя покупателя
  - `_rating: int` - оценка от 1 до 5
  - `_comment: str` - текст отзыва
  - `_created_at: datetime` - дата создания
- Валидация:
  - Рейтинг должен быть от 1 до 5 (используй setter)
  - Комментарий не должен быть пустым
- Методы:
  - `is_positive()` - возвращает `True` если рейтинг >= 4
  - `to_dict()` - преобразует в словарь

**Дополнительно:**
- Добавь в класс `Product` список отзывов: `_reviews: List[Review] = []`
- Добавь метод `add_review(review: Review)`
- Добавь метод `get_average_rating()` - средний рейтинг товара

---

### 8️⃣ Создай абстрактный класс PaymentMethod

**Задача:**
- Создай абстрактный класс для способов оплаты:
  - Абстрактный метод `process_payment(amount: float) -> bool`
  - Абстрактный метод `get_payment_type() -> str`
- Создай 3 конкретных класса:
  - `CreditCardPayment` - оплата картой
  - `CashPayment` - оплата наличными
  - `CryptocurrencyPayment` - оплата криптовалютой
- Каждый класс должен реализовать свою логику оплаты (можно просто print)

**Проверка:**
```python
payments = [
    CreditCardPayment(card_number="1234-5678-9012-3456"),
    CashPayment(),
    CryptocurrencyPayment(wallet_address="0x123...")
]

for payment in payments:
    print(f"Способ оплаты: {payment.get_payment_type()}")
    payment.process_payment(1000)
```

---

### 9️⃣ Добавь метод в Order для применения промокода

**Задача:**
- Создай класс `PromoCode`:
  - `code: str` - код промокода
  - `discount_percent: float` - процент скидки
  - `min_order_amount: float` - минимальная сумма заказа
  - `is_active: bool` - активен ли промокод
- Добавь в класс `Order`:
  - Поле `_applied_promo: Optional[PromoCode] = None`
  - Метод `apply_promo_code(promo: PromoCode) -> bool`:
    - Проверяет, активен ли промокод
    - Проверяет, достаточна ли сумма заказа
    - Если да - применяет скидку и возвращает `True`
  - Измени метод `get_total()` чтобы учитывал промокод

---

### 🔟 Создай систему уведомлений (Observer Pattern)

**Задача:**
- Создай абстрактный класс `NotificationService`:
  - Абстрактный метод `send_notification(message: str, recipient: str)`
- Создай конкретные классы:
  - `EmailNotification` - отправка email
  - `SMSNotification` - отправка SMS
  - `PushNotification` - push-уведомление
- Добавь в класс `Order`:
  - Список `_notification_services: List[NotificationService]`
  - Метод `add_notification_service(service: NotificationService)`
  - При изменении статуса заказа - отправлять уведомления через все сервисы

**Пример:**
```python
order = Order("Иван", "ivan@mail.ru")
order.add_notification_service(EmailNotification())
order.add_notification_service(SMSNotification())

order.change_status(OrderStatus.SHIPPED)
# Отправит уведомления через email и SMS
```

---

## 🎁 Бонусное задание (необязательно)

### Создай FastAPI endpoint для отзывов

**Задача:**
- Добавь в `fastapi_app.py` endpoints:
  - `POST /products/{product_id}/reviews` - создать отзыв
  - `GET /products/{product_id}/reviews` - получить все отзывы товара
  - `GET /products/{product_id}/rating` - получить средний рейтинг
- Создай Pydantic модели для валидации

---

## ✅ Критерии выполнения

- [ ] Все классы правильно используют наследование
- [ ] Реализованы все абстрактные методы
- [ ] Используется инкапсуляция (приватные поля + @property)
- [ ] Код работает без ошибок
- [ ] Добавлены docstrings к классам и методам
- [ ] Код следует принципам SOLID и DRY

---

## 💡 Подсказки

1. Импортируй классы из `oop_10.py`:
   ```python
   from lesson_10_OOP_advanced.oop_10 import Product, Inventory, Order, ProductCategory
   ```

2. Для создания новой категории товара можешь расширить `ProductCategory` enum

3. Используй `super()` для вызова методов родительского класса

4. Не забывай про валидацию данных в setters

5. Тестируй каждый класс отдельно перед интеграцией

---

## 🚀 Запуск

```bash
# Активируй виртуальное окружение
source .venv/bin/activate

# Запусти свой файл
python lesson_10_OOP_advanced/homework/homework_10.py
```

---

**Удачи! 🎉**
